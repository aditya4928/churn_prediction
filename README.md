It an end to end project for churn prediction, that customer will stay in the the bank or leave it we have use normalization techniques, hypertuning, artificial neural network to perform the prediction.
